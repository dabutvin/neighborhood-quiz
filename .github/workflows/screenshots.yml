name: PR Screenshots

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'LICENSE'
      - '.gitignore'
  # A hand crank, for when the run a pull request should have started never
  # arrives — a runner shortage, a cancelled queue, an Actions hiccup. Give it
  # the pull request number and it posts the shots to that one as usual.
  workflow_dispatch:
    inputs:
      pr:
        description: Pull request number to post the screenshots to
        required: true

concurrency:
  group: screenshots-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write

jobs:
  screenshots:
    name: Capture App Screenshots
    runs-on: macos-26
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          XCODE=$(ls -d /Applications/Xcode_26*.app 2>/dev/null | sort -V | tail -1 || true)
          if [ -n "$XCODE" ]; then sudo xcode-select -s "$XCODE/Contents/Developer"; fi
          xcodebuild -version

      - name: Install XcodeGen
        run: HOMEBREW_NO_AUTO_UPDATE=1 brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      # The destination is generic so that nothing here has to wait on a
      # simulator, and `ARCHS` keeps a generic destination from compiling every
      # architecture the simulator platform has.
      - name: Build for Simulator
        run: |
          set -o pipefail
          xcodebuild build \
            -project NeighborhoodQuiz.xcodeproj \
            -scheme NeighborhoodQuiz \
            -destination "generic/platform=iOS Simulator" \
            -configuration Debug \
            -derivedDataPath DerivedData \
            ARCHS="$(uname -m)" \
            COMPILER_INDEX_STORE_ENABLE=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | tee build.log
          if grep -q "BUILD FAILED" build.log; then exit 1; fi

      # After the build rather than beside it: booting takes every core a runner
      # has, and sharing them made the build four times slower for no gain.
      - name: Wake up the iPhone simulator
        id: simulator
        uses: ./.github/actions/simulator

      - name: Capture iPhone screenshots
        env:
          UDID: ${{ steps.simulator.outputs.udid }}
        run: |
          set -euo pipefail
          mkdir -p screenshots

          BUNDLE_ID="com.nycneighborhoodsquiz.app"

          APP_PATH=$(find DerivedData/Build/Products/Debug-iphonesimulator -name "*.app" -maxdepth 1 | head -1)
          if [ -z "$APP_PATH" ]; then
            echo "ERROR: Could not find built .app bundle"
            exit 1
          fi
          echo "Installing: $APP_PATH"
          xcrun simctl install "$UDID" "$APP_PATH"

          # Each screen is shot in both appearances off a single launch: the views
          # read the colour scheme out of the environment, so flipping the
          # simulator under a running app re-draws it, and the two shots then show
          # the same map rather than two rolls of the dice. The launch arguments
          # are the ones `Screen` understands.
          #
          # No argument at all is what a person gets, which is now the menu with
          # whatever they have actually earned. The gallery asks for `-quiz-menu`
          # instead, which is the same screen with a wallet part-way to Brooklyn,
          # so the shot has something on it on a simulator that has never played.
          #
          # The `-quiz-*` runs play a fixed round forward to each state the game
          # can be in — picked and waiting on the button, two goes gone, the
          # island half filled in, a place the round gave away, the end of it —
          # which is where the bugs will be. They go through the real rules, so
          # these are photographs of the game rather than mock-ups of it.
          #
          # The `-map-*` runs ask for the map on its own, where a regression in
          # the drawing shows up plainly with nothing else on the screen.
          # `-map-brooklyn` is the second borough, drawn north-up rather than
          # turned to its grid, because it has half a dozen grids and no one of
          # them is the borough; a regression in the loader or the projection
          # that only the other file would catch is what this shot is for.
          #
          # `-boroughs` is the ladder, seeded with enough money for Brooklyn so
          # the shot shows the rung that has something to say — a live Unlock
          # button now that Brooklyn is drawn, where it used to be the one
          # place the game had to admit the map behind it was not built.
          #
          # Every shot is checked before it counts, because one of these came
          # back as a white launch screen once and went up on the pull request
          # looking like any other file in the list. A gallery whose whole job
          # is to make a rendering regression obvious must not be able to pass
          # a blank page quietly.
          #
          # Size is the tripwire. Every screen in this app is paper and paper
          # has grain on every pixel of it, so a real capture never compresses
          # down to nothing; the blank one was seventy-five kilobytes.
          #
          # The threshold is well under the smallest real shot rather than just
          # under it, because this is here to catch a page that never drew, not
          # to insist every screen be a busy one. The boroughs ladder is flat by
          # design and still perfectly real — a bar that assumed a map behind
          # every screen would fail it and teach everyone to ignore the alarm.
          MIN_SHOT_BYTES=300000

          looks_blank() {
            local bytes
            bytes=$(wc -c < "$1" | tr -d ' ')
            [ "$bytes" -lt "$MIN_SHOT_BYTES" ]
          }

          capture() {
            local order="$1"
            local screen="$2"
            shift 2

            local light dark attempt
            light=$(printf 'screenshots/%02d_%s_light.png' "$order" "$screen")
            dark=$(printf 'screenshots/%02d_%s_dark.png' "$((order + 1))" "$screen")

            # Three goes, waiting longer each time. Building the map is the
            # slow part of a launch and a loaded runner can still be at it
            # when the first shutter goes.
            for attempt in 1 2 3; do
              xcrun simctl terminate "$UDID" "$BUNDLE_ID" 2>/dev/null || true
              xcrun simctl ui "$UDID" appearance light
              xcrun simctl launch "$UDID" "$BUNDLE_ID" "$@"
              sleep $((2 + attempt * 2))
              xcrun simctl io "$UDID" screenshot "$light"
              if looks_blank "$light"; then
                echo "::warning::$light came out blank on attempt $attempt; relaunching"
                continue
              fi

              xcrun simctl ui "$UDID" appearance dark
              sleep 2
              xcrun simctl io "$UDID" screenshot "$dark"
              if looks_blank "$dark"; then
                echo "::warning::$dark came out blank on attempt $attempt; relaunching"
                continue
              fi

              xcrun simctl terminate "$UDID" "$BUNDLE_ID" 2>/dev/null || true
              echo "Captured: $light, $dark"
              return 0
            done

            echo "ERROR: $screen never drew — three launches came back blank."
            exit 1
          }

          capture  1 menu            -quiz-menu
          capture  3 quiz            -quiz-asking
          capture  5 quiz_picked     -quiz-picked
          capture  7 quiz_narrowing  -quiz-narrowing
          capture  9 quiz_filling    -quiz-filling
          capture 11 quiz_missed     -quiz-missed
          capture 13 quiz_over       -quiz-over
          capture 15 map             -map
          capture 17 map_zoomed      -map-zoomed
          capture 19 map_neighborhood -map-neighborhood
          capture 25 map_brooklyn    -map-brooklyn
          capture 27 boroughs        -boroughs
          capture 29 settings        -settings

          xcrun simctl status_bar "$UDID" clear
          xcrun simctl shutdown "$UDID" 2>/dev/null || true

          echo "Screenshots captured:"
          ls -la screenshots/

      - name: Upload screenshots artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-screenshots-pr-${{ github.event.pull_request.number || inputs.pr }}
          path: screenshots/
          retention-days: 30

      - name: Post screenshots to PR
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');

            const prNumber = context.payload.pull_request?.number
              ?? Number(context.payload.inputs.pr);
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const branch = 'ci-screenshots';
            const screenshotsDir = 'screenshots';

            // Ensure the ci-screenshots branch exists
            async function ensureBranch() {
              try {
                await github.rest.repos.getBranch({ owner, repo, branch });
              } catch (e) {
                if (e.status === 404) {
                  const defaultBranch = await github.rest.repos.getBranch({
                    owner, repo, branch: 'main'
                  });
                  await github.rest.git.createRef({
                    owner, repo,
                    ref: `refs/heads/${branch}`,
                    sha: defaultBranch.data.commit.sha
                  });
                  core.info(`Created branch: ${branch}`);
                } else {
                  throw e;
                }
              }
            }

            // Run `worker` over `items`, `limit` of them in flight at a time.
            async function inParallel(items, limit, worker) {
              const results = new Array(items.length);
              let next = 0;
              await Promise.all(
                Array.from({ length: Math.min(limit, items.length) }, async () => {
                  while (next < items.length) {
                    const i = next++;
                    results[i] = await worker(items[i], i);
                  }
                })
              );
              return results;
            }

            await ensureBranch();

            const files = fs.readdirSync(screenshotsDir)
              .filter(f => f.endsWith('.png'))
              .sort();

            // A blob apiece, several at a time, and then one commit holding all of
            // them. Writing the files one at a time through the contents API meant
            // a round trip and a commit each; this is one commit and a few seconds.
            const blobs = await inParallel(files, 8, async (file) => {
              const content = fs.readFileSync(path.join(screenshotsDir, file));
              const { data } = await github.rest.git.createBlob({
                owner, repo,
                content: content.toString('base64'),
                encoding: 'base64'
              });
              core.info(`Uploaded ${file}`);
              return {
                path: `pr-${prNumber}/${file}`,
                mode: '100644',
                type: 'blob',
                sha: data.sha
              };
            });

            // Every pull request writes to this one branch, so the commit is built
            // on whatever the tip is at the moment it lands. If somebody else's run
            // got there first, build it again on top of theirs rather than over it.
            async function commitShots() {
              const ref = await github.rest.git.getRef({
                owner, repo, ref: `heads/${branch}`
              });
              const parent = ref.data.object.sha;
              const parentCommit = await github.rest.git.getCommit({
                owner, repo, commit_sha: parent
              });
              const tree = await github.rest.git.createTree({
                owner, repo,
                base_tree: parentCommit.data.tree.sha,
                tree: blobs
              });
              const commit = await github.rest.git.createCommit({
                owner, repo,
                message: `Update screenshots for PR #${prNumber}`,
                tree: tree.data.sha,
                parents: [parent]
              });
              await github.rest.git.updateRef({
                owner, repo,
                ref: `heads/${branch}`,
                sha: commit.data.sha
              });
            }

            for (let attempt = 1; ; attempt++) {
              try {
                await commitShots();
                break;
              } catch (e) {
                if (e.status !== 422 || attempt === 5) throw e;
                core.info(`${branch} moved under us; trying again (${attempt})`);
              }
            }

            const images = files.map(file => ({
              name: file,
              url: `https://raw.githubusercontent.com/${owner}/${repo}/${branch}/pr-${prNumber}/${file}`
            }));

            // A pull request event knows all of this; a hand-cranked run has to be told
            // the number and look the rest up.
            const pr = context.payload.pull_request
              ?? (await github.rest.pulls.get({ owner, repo, pull_number: prNumber })).data;

            const commitSha = pr.head.sha.substring(0, 7);
            const cacheBust = Date.now();

            let body = `## 📱 App Screenshots\n\n`;
            body += `> Automatically captured from commit \`${commitSha}\` on **${pr.head.ref}**\n\n`;

            // A screen is named <order>_<screen>_<appearance>.png and gets its own
            // light/dark row; anything else is shown on its own.
            const screens = [];
            const loose = [];
            const screenNamed = (name) => {
              let screen = screens.find(s => s.name === name);
              if (!screen) { screen = { name }; screens.push(screen); }
              return screen;
            };
            for (const img of images) {
              const pair = img.name.match(/^\d+_(.+)_(light|dark)\.png$/);
              if (pair) { screenNamed(pair[1])[pair[2]] = img.url; continue; }
              loose.push(img);
            }

            const shot = (url) => `<img src="${url}?v=${cacheBust}" width="300" />`;
            const cell = (url) => url ? shot(url) : '—';

            const heading = (name) => {
              const label = name.replace(/_/g, ' ');
              return `${label.charAt(0).toUpperCase()}${label.slice(1)}`;
            };

            for (const screen of screens) {
              body += `### ${heading(screen.name)}\n\n`;
              body += `| Light Mode | Dark Mode |\n`;
              body += `|:---:|:---:|\n`;
              body += `| ${cell(screen.light)} | ${cell(screen.dark)} |\n\n`;
            }

            for (const img of loose) {
              body += `### ${img.name.replace('.png', '').replace(/_/g, ' ')}\n`;
              body += `<img src="${img.url}?v=${cacheBust}" width="350" />\n\n`;
            }

            body += `\n<sub>📦 Full-resolution screenshots available in the <a href="${pr.html_url}/checks">workflow artifacts</a>.</sub>\n`;
            body += `\n<!-- pr-screenshots-bot -->`;

            // Find existing bot comment to update, or create a new one
            const watermark = '<!-- pr-screenshots-bot -->';
            const comments = await github.rest.issues.listComments({
              owner, repo, issue_number: prNumber
            });

            const existing = comments.data.find(c => c.body && c.body.includes(watermark));

            if (existing) {
              await github.rest.issues.updateComment({
                owner, repo,
                comment_id: existing.id,
                body
              });
              core.info(`Updated existing comment #${existing.id}`);
            } else {
              await github.rest.issues.createComment({
                owner, repo,
                issue_number: prNumber,
                body
              });
              core.info('Created new PR comment with screenshots');
            }
